|| Link Notifier

リンクの末尾にアイコンを表示することで、リンクのタイプやファイル種別を視覚化します。
例えばPDFリンクの末尾にPDFアイコンを表示することで、開いてブラウザがフリーズする事態を防ぐことができるでしょう。

このフィルタはリストを使用します。
リスト名は「LinkNotifier」です。

現在のバージョンでは、各自で16*16pxの画像を用意しないと動作しません。
この仕様は将来的に改訂されます。